# Copy to .env and `export $(grep -v '^#' .env | xargs)` if you want to
# source these instead of passing CLI flags. Never commit a real .env file.
#
# Prefer the interactive prompts in client.py for SSID/password — only
# use these for scripting/CI where the shell environment is already
# otherwise secured.

WIFI_PORTAL_SSID=
WIFI_PORTAL_PASSWORD=
WIFI_PORTAL_TOKEN=
